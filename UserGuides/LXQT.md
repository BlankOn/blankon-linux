# LXQT

## Install

Although we can install `lxqt` in various way (just `lxqt-core` or `lxqt`), it is recommended to install via metapackage `task-lxqt-desktop`

```
sudo apt install task-lxqt-desktop
```
